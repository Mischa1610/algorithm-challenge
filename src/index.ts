import { Transform } from './Transform.class';

console.log('Hello to my Algorithms Challenge!');
console.log('---------------------------------');

const N = 196;

console.log(`Given number is ${N}`);

console.log(`is ${N} a Palindrome: ${Transform.isPalindrome(N)}`);

console.log(`${N} as reverse Number: ${Transform.reverseNumber(N)}`);

console.log(`${N} as Palindrome: ${Transform.palindrome(N)}`);
